git commit -m "feat: implement complete RAG pipeline with document ingestion and vector search

- Added document ingestion system (ingest.js)
  - Reads files from uploads directory
  - Splits documents into overlapping chunks
  - Generates embeddings using Gemini API
  - Stores vector representations in memory (vectorDb)

- Implemented vector search engine (queringVectors.js)
  - Added cosine similarity function for embedding comparison
  - Retrieves top-K most relevant document chunks
  - Supports query embedding generation using Gemini

- Created pipeline test runner (server.js)
  - Orchestrates ingestion and querying flow
  - Tests full RAG pipeline end-to-end
  - Logs ingestion stats and search results

- Improved architecture
  - Removed global state dependency issues
  - Ensured ingestion returns vector database explicitly
  - Clean separation between ingestion and retrieval logic

This commit establishes a working prototype of a Retrieval-Augmented Generation system using Node.js and Gemini embeddings."






git commit -m "feat: add multimodal RAG system with image-based query support

- Added image understanding pipeline using Gemini Vision model
  - Implemented identifyImageSubject() to analyze uploaded images
  - Converts images into descriptive text labels using AI vision model

- Integrated image-to-text retrieval system
  - Converts image into base64 format
  - Extracts semantic subject from image
  - Uses extracted subject to query vector database

- Updated server pipeline (server.js)
  - Supports both text-based and image-based queries
  - Combines traditional RAG with multimodal search capability
  - Separates text search and image-driven retrieval flow

- Improved system architecture
  - Introduced cross-modal retrieval (image → text embeddings)
  - Fixed async handling and function naming issues
  - Cleaned duplicate variables and improved flow structure

This commit upgrades the project from a basic RAG system to a multimodal retrieval system capable of understanding both text and images."





git commit -m "feat: add multimodal RAG chat system with image understanding and conversational AI

- Introduced chat layer (chat.js) for LLM-based response generation
  - Integrated Gemini generative model for context-aware answers
  - Added support for chat history for conversational memory
  - Combined retrieved vector context with user queries

- Enhanced server pipeline (server.js)
  - Integrated text-based RAG search flow
  - Added image understanding pipeline using Gemini Vision
  - Combined image-to-text conversion with vector retrieval
  - Integrated chat system with RAG context

- Improved system architecture
  - Unified text RAG, image RAG, and chat into single pipeline
  - Ensured async flow consistency across all modules
  - Fixed integration issues between ingestion, retrieval, and generation

This commit transforms the project into a multimodal RAG chatbot capable of understanding text, images, and generating contextual AI responses."





git commit -m "chore: update project configuration and entry point

- Changed main entry from rag.js to server.js
- Added npm scripts for better workflow:
  - npm start → runs main server
  - npm run ingest → runs ingestion pipeline
- Updated project metadata (description, keywords, author)
- Improved project structure for multimodal RAG system

This commit aligns package configuration with the current architecture, which now includes ingestion, vector search, image understanding, and chat system."





git commit -m "feat: add structured response generator for RAG system

- Introduced responseGenerator.js to handle LLM-based answer generation
- Combined retrieved vector chunks with user query for context-aware responses
- Implemented strict JSON output format:
  {
    "context-about": "<topic>",
    "response": "<final answer>"
  }

- Added prompt engineering for Gemini model to ensure structured output
- Implemented fallback handling for invalid JSON responses
- Enhanced RAG pipeline by adding final generation layer after retrieval

This commit completes the full RAG architecture:
ingestion → retrieval → generation → structured response output for frontend integration"





git commit -m "feat: convert RAG pipeline into REST API with modular routing

- Integrated Express.js server to expose backend functionality via HTTP APIs
- Implemented POST /api/search/text endpoint for semantic search + LLM response generation
- Refactored project structure:
  - Moved AI logic into src/rag module
  - Introduced src/routes for API route handling
- Added searchRoutes module with dependency injection (vectorDb)
- Initialized vector database at server startup for efficient querying
- Connected retrieval (vector search) with generation (LLM) to produce structured JSON responses

This marks the transition from a standalone script to a scalable API-based RAG backend, ready for frontend integration."


request:

POST    http://localhost:3000/api/search/text

{
  "query": "What is Sigiriya?"
}


response:

{
    "success": true,
    "data": {
        "context-about": "Sigiriya",
        "response": "Sigiriya, also known as Lion Rock, is a historical site and UNESCO World Heritage Site in Sri Lanka. Built in the 5th century AD by King Kashyapa, it served as a royal palace and fortress featuring advanced urban planning, water gardens, and frescoes."
    }
}